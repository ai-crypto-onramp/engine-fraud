# Runtime dependencies for the fraud-detection service.
# Dev/test/training-only deps live in requirements-dev.txt.
fastapi
uvicorn[standard]
aiokafka
redis
prometheus-client
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
opentelemetry-instrumentation-fastapi
httpx

# Model serving: joblib + sklearn/xgboost/shap are needed to load a real
# model artifact (RealModel in src/fraud_detection/scoring.py). These are
# heavy (~1.5 GB) but required when MODEL_PATH/MODEL_REGISTRY_URL is set.
# In DEV_MODE the StubModel is used and these are not imported.
joblib
scikit-learn
xgboost
shap

# Postgres driver for the audit/event store. psycopg[binary] is preferred;
# asyncpg is a legacy holdover and can be dropped once db.py is migrated.
psycopg
psycopg[binary]
asyncpg