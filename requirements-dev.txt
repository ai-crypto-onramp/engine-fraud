# Dev/test/training-only dependencies. Not installed in the runtime image.
-r requirements.txt

# Testing
pytest
pytest-asyncio
pytest-cov

# Lint / type-check
ruff
mypy

# Training-only (mlflow is imported only in src/fraud_detection/training/* and
# src/fraud_detection/registry.py with a guarded `try: import mlflow`).
mlflow

# Optional feature store (src/fraud_detection/feature_store.py guards the import).
# Pin feast to a modern release: feast <=0.39 pulls pandavro~=1.5.0 which requires
# pandas<2 and conflicts with shap's pandas, causing pip to backtrack down to
# feast 0.28 whose setup.py is broken on modern pip (CI failure). >=0.40 dropped
# the pandavro dependency.
feast[redis]>=0.40